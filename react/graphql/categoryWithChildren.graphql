query {
  categories(treeLevel: 2) {
    id
    name
    text: name
    href
    slug
    hasChildren
    children {
      id
      name
      text: name
      href
      slug
      hasChildren
      children {
        id
        name
        text: name
        href
        slug
      }
    }
  }
}